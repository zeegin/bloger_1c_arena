📊 <b>Топ каналов:</b>

{% for entry in entries -%}
{{ loop.index }}. <a href="{{ entry.tg_url }}">{{ entry.title }}</a> — <b>{{ entry.rating | round(0) | int }}</b> (игр: {{ entry.games }}, побед: {{ entry.wins }})
{% endfor -%}

🕹️ Сыграно игр: <b>{{ stats.games }}</b>
👥 Игроков: <b>{{ stats.players }}</b>
