# Взаимодействие бота и wireframe

Этот документ описывает каждый пользовательский «экран» (сообщение + клавиатура) и переходы между ними.

## 1. Главное меню
- **Когда**: `/start`, завершение deathmatch, возвраты.
- **Сообщение:**
  ```
  Привет! Я собираю рейтинг каналов по системе Elo.
  Выбирай, какой из двух каналов лучше.
  ```
- **Кнопки (`main_menu_kb`)**:
  | Текст | Callback |
  | --- | --- |
  | `✅ Арена` | `menu:play` |
  | `🔥 Deathmatch` | `menu:deathmatch` |
  | `📊 Рейтинг` | `menu:top` |

## 2. Классический дуэль (шахматное голосование)
- **Как попасть**: `✅ Арена` из главного меню.
- **Сообщение:**
  ```
  Выбери канал из рейтинга <b>{band}</b>:

  <b>A:</b> <a href="{url_a}">{title_a}</a>
  {desc_a}

  <b>B:</b> <a href="{url_b}">{title_b}</a>
  {desc_b}
  ```
  Перед текстом отправляется одно фото с аватарками A/B и случайным «VS».
- **Кнопки (`duel_kb`)**:
  | Текст | Callback |
  | --- | --- |
  | `👍 Выбрать A` | `vote:{token}:{a_id}:{b_id}:A` |
  | `👍 Выбрать B` | `vote:{token}:{a_id}:{b_id}:B` |
  | `🤝 Ничья` (если включено) | `vote:{token}:{a_id}:{b_id}:D` |
  | `📊 Рейтинг` | `menu:top` |
- **Переход**: после валидации токена бот вызывает `send_duel` вновь (новая пара).

## 3. Deathmatch
- **Как попасть**: `🔥 Deathmatch` из меню (минимум игр берётся из `DeathmatchService.min_classic_games`).
- **Раундовое сообщение:**
  ```
  🔥 Deathmatch стартует! Выбирай победителя.              # первый раунд
  🔥 Deathmatch продолжается! Чемпион ждёт нового соперника.  # последующие

  <b>{label_a}:</b> <a href="{url_a}">{title_a}</a>
  {desc_a}

  <b>{label_b}:</b> <a href="{url_b}">{title_b}</a>
  {desc_b}
  ```
  На первом раунде `label_a`/`label_b` — `A`/`B`, далее — `👑 Чемпион` и `🥊 Претендент`.
- **Кнопки (`deathmatch_kb`)**:
  | Текст | Callback |
  | --- | --- |
  | `👑 Выбрать A` | `dmvote:{token}:{a_id}:{b_id}:A` |
  | `🥊 Выбрать B` | `dmvote:{token}:{a_id}:{b_id}:B` |
  | `📊 Рейтинг` | `menu:top` |
- **Если игр меньше минимума:** бот отвечает  
  ```
  🔥 Deathmatch доступен после {min_games} классических игр. Продолжай голосовать в арене! Осталось сыграть: <b>{remaining}</b>.
  ```
  (клавиатура `main_menu_kb`).
- **Если в топе недостаточно каналов:**  
  ```
  Недостаточно каналов в топе, чтобы начать deathmatch.
  ```
  (клавиатура `main_menu_kb`).
- **Финал** (когда претенденты закончились):
  ```
  🏆 Deathmatch завершён!
  <b>Победитель:</b> <a href="{url}">{title}</a>
  Он сохранён как твой любимчик.
  ```
  (клавиатура `main_menu_kb`).
- **Ошибка/устаревший раунд:**  
  ```
  Раунд устарел. Нажми «🔥 Deathmatch», чтобы начать заново.
  ```
  или  
  ```
  Deathmatch ещё не запущен. Нажми «🔥 Deathmatch» в меню.
  ```

## 4. Рейтинг Top 20
- **Как попасть**: `📊 Рейтинг` (`menu:top`) или из других подсекций по кнопке «⬅️ Топ 20».
- **Сообщение:**
  ```
  📊 <b>Топ каналов:</b>
  Шахматная рейтинговая система Elo
  1. <a href="{url}">{title}</a> — <b>{rating}</b> (игр: {games})
  ...

  🕹️ Сыграно игр: <b>{games_total}</b>
  👥 Игроков: <b>{players}</b>
  ```
- **Кнопки (`rating_kb("top20")`)**:
  | Текст | Callback |
  | --- | --- |
  | `📈 TOP 100` | `top:100` |
  | `❤️ Любимчики` | `top:favorites` |
  | `✅ Арена` | `menu:play` |
  | `🔥 Deathmatch` | `menu:deathmatch` |

## 5. Рейтинг TOP 100 / Все каналы
- **Как попасть**: кнопка `📈 TOP 100`.
- **Сообщение:**
  ```
  📈 <b>{'TOP 100' или 'Все каналы'}</b>
  1. <a href="{url}">{title}</a> — <b>{rating}</b> (игр: {games})
  ...
  ```
  Если каналов меньше 100, заголовок меняется на «Все каналы».
- **Кнопки (`rating_kb("top100")`)**:
  | Текст | Callback |
  | --- | --- |
  | `⬅️ Топ 20` | `top:back` |
  | `✅ Арена` | `menu:play` |
  | `🔥 Deathmatch` | `menu:deathmatch` |

## 6. Любимчики
- **Как попасть**: кнопка `❤️ Любимчики`.
- **Сообщение:**
  ```
  ❤️ <b>Рейтинг Deathmatch:</b>:
  1. <a href="{url}">{title}</a> — <b>{fans}</b>
  ...

  🕹️ Deathmatch игр: <b>{dm_games}</b>
  👥 Deathmatch игроков: <b>{dm_players}</b>
  
  ❤️ Твой любимчик: <a href="{fav_url}">{fav_title}</a>   # если задан
  ```
- **Кнопки (`rating_kb("favorites")`)**:
  | Текст | Callback |
  | --- | --- |
  | `⬅️ Топ 20` | `top:back` |
  | `✅ Арена` | `menu:play` |
  | `🔥 Deathmatch` | `menu:deathmatch` |

## 7. Служебные экраны
### 7.1 Устаревший токен / повторное нажатие
- **Когда**: неверный callback, истёкший токен или повторное нажатие.
- **Сообщение (варианты):**
  ```
  Голос уже учтён. Запроси новый дуэль.
  ```
  ```
  Голос уже учтён. Ждём следующий вызов!
  ```
  ```
  Раунд устарел. Нажми «🔥 Deathmatch», чтобы начать заново.
  ```
  ```
  Deathmatch ещё не запущен. Нажми «🔥 Deathmatch» в меню.
  ```
- **Кнопки**:
  | Текст | Callback |
  | --- | --- |
  | `✅ Арена` | `menu:play` |
  | `🔥 Deathmatch` | `menu:deathmatch` |

### 7.2 Недостаточно каналов / пустая база
- **Когда**: база пуста или в ней <2 каналов.
- **Сообщение:**
  ```
  Нужно минимум 2 канала в базе.
  ```
  или
  ```
  Пока нет каналов в базе.
  ```
- **Кнопки**: `main_menu_kb` (см. раздел 1).

## 8. Секретный приз
- **Когда**: пользователь достигает 350 или 700 классических игр (арена). Пороговые ссылки задаются через `ARENA_REWARD_350_URL` / `ARENA_REWARD_700_URL`.
- **Сообщение:**
  ```
  🎁 Спасибо за {games} игр в арене!
  Вот секретный подарок: {url}
  ```
- **Кнопки**: `main_menu_kb`.
- После выдачи приз помечается как полученный, поэтому повторно сообщение не показывается. Для пользователей, у которых в базе уже больше игр, приз появляется после следующей классической игры.

## Диаграмма переходов
```
[Главное меню]
  ├─(Арена)→ [Классический дуэль] ──(голос)→ обратно к дуэли
  ├─(Deathmatch)→ [Раунд DM] ──…→ [Финал]
  └─(Рейтинг)→ [Top 20] ──┬─(TOP100)→ [Top100/All]
                           └─(Любимчики)→ [Favorites]
```
